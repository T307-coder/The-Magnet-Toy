# This file is used by the starcatcher.us update server to pull changelogs for mod releases
# Erase and replace the changelog for every release, do not just append changes to the end
# Lines starting with # and empty lines are ignored
v1.2.4 — Unified Q key for non-solid charge fields, per-element eSrc gating.
Q key now controls field production for ALL non-solid charged particles (powders, liquids, gases, plasma).
Solids always produce fields when electricity is enabled (Y key).
Per-element eSrc writes and chargeContact now respect Q key for non-solids.
BRMT/BREC/MERC induction unified with shared magnetism_tryInduction.
Inline Lorentz and ferromagnetic pull replaced with shared header functions.
