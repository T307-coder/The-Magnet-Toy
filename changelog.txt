# This file is used by the starcatcher.us update server to pull changelogs for mod releases
# Erase and replace the changelog for every release, do not just append changes to the end
# Lines starting with # and empty lines are ignored
v1.2.7 — New induction direction fix: both axes independent, allowing diagonal electron drift.
Electron drift now properly perpendicular to B-field gradient in all directions.
Previously dominant-axis approach quantized drift to cardinal directions only.
README updated with full feature documentation (O/Z/S keys, semiconductor exclusion, ferromagnet dedup).
